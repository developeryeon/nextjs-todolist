export interface TodoType {
	id: string;
	title: string;
	contents: string;
	isDone: boolean;
}

export type CompanyType = {
	name: string;
	description: string;
	image: string;
};

export type isDoneType = {
	isDone: boolean;
};
