export interface TodoType {
	id: string;
	title: string;
	contents: string;
	isDone: boolean;
}

export interface companyType {
	name: string;
	desctiption: string;
	image: string;
}

export type isDoneData = {
	isDone: boolean;
};
